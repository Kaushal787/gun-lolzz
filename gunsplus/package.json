{
  "name": "gunsplus",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "web:dev": "npm run dev --workspace @gunsplus/web",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.1"
  }
}
